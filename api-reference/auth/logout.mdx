---
title: 'Logout'
openapi: 'POST /api/auth/logout'
---

End the current session and revoke the JWT token.

## Request

<CodeGroup>

```bash cURL
curl -X POST https://api.fon.app/api/auth/logout \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

```javascript JavaScript
const response = await fetch('https://api.fon.app/api/auth/logout', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_JWT_TOKEN'
  }
});
```

</CodeGroup>

## Response

```json
{
  "success": true
}
```

## Notes

- The JWT token is added to a blacklist and can no longer be used
- Session cookies are cleared
- API tokens are **not** affected by logout
